import { combineReducers } from 'redux';

export default combineReducers({
    replaceMe: () => 'YooHoo!!' // We just add this to trick the broweser into thinking we do have some reducers, to make that err msg go away!
});